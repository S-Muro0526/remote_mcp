# MCP Remote Server (Node.js - Express)

## Deploy to Railway
1. Fork this repo → push to GitHub  
2. Railway → New Project → Deploy from GitHub  
3. Confirm start command: `npm start`  
4. Public URL: `https://xxx.up.railway.app/mcp`

## Tools
- `ping` → returns `pong`
- `echo` → returns params
